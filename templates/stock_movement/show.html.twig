{% extends 'base.html.twig' %}

{% block title %}StockMovement{% endblock %}

{% block body %}
    <h1>StockMovement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_movement.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ stock_movement.quantity }}</td>
            </tr>
            <tr>
                <th>MovementType</th>
                <td>{{ stock_movement.movementType }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ stock_movement.date ? stock_movement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ stock_movement.reason }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_movement_index') }}">back to list</a>

    <a href="{{ path('app_stock_movement_edit', {'id': stock_movement.id}) }}">edit</a>

    {{ include('stock_movement/_delete_form.html.twig') }}
{% endblock %}
